% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tsqca_core.R, R/tsqca_fiss_core.R
\name{classify_term_conditions}
\alias{classify_term_conditions}
\title{Classify each (condition, status) pair in an intermediate term as
core or peripheral}
\usage{
classify_term_conditions(interm_term, parsim_map, conditions)

classify_term_conditions(interm_term, parsim_map, conditions)
}
\arguments{
\item{interm_term}{Character. Single intermediate-solution term.}

\item{parsim_map}{Named list returned by \code{extract_cond_status_map()}
for the parsimonious solution.}

\item{conditions}{Character vector of all condition names.}
}
\value{
Data frame with columns:
  \code{condition}, \code{status} ("present"/"absent"/"dontcare"),
  \code{type} ("core"/"peripheral"/"dontcare").

Data frame with columns:
  \code{condition}, \code{status} ("present"/"absent"/"dontcare"),
  \code{type} ("core"/"peripheral"/"dontcare").
}
\description{
A condition is **core** when it appears with the same presence/absence
status in at least one term of the parsimonious solution.
A condition is **peripheral** when it appears in the intermediate term
but NOT (with the same status) in any parsimonious term.

A condition is **core** when it appears with the same presence/absence
status in at least one term of the parsimonious solution.
A condition is **peripheral** when it appears in the intermediate term
but NOT (with the same status) in any parsimonious term.
}
\keyword{internal}
